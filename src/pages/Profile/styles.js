import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({

  profilePhoto: {
    width: 100,
    height: 100,
  },

  profileName: {
    fontSize: 18,
    fontWeight: "bold",
    color: "#885500",
    marginTop: 10,
  },

  listRegisters: {
    flex: 1,
    paddingTop: 30,
  },

});
